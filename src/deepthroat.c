
#include "deepthroat.h"


inline double roundp(double x, int precise);

inline int array_fany(double array[], size_t start, size_t stop);

inline int array_fall(double array[], size_t start, size_t stop);

inline int array_iany(int array[], size_t start, size_t stop);

inline int array_iall(int array[], size_t start, size_t stop);