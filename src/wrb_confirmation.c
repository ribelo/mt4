
#include "wrb_confirmation.h"

inline signal conf_a(ohlc *candle, size_t i);

inline signal conf_b(ohlc *candle, size_t i);

inline signal conf_c(ohlc *candle, size_t i);

inline signal conf_d(ohlc *candle, size_t i);

inline signal conf_e(ohlc *candle, size_t i);

// inline signal conf_f(ohlc *candle, size_t i);

// inline signal conf_g(ohlc *candle, size_t i);

inline signal conf_h1(ohlc *candle, size_t i, size_t n, size_t contraction);

inline signal conf_h2(ohlc *candle, size_t i, size_t n, size_t contraction);

inline signal conf_h3(ohlc *candle, size_t i, size_t n, size_t contraction);

inline signal conf_h4(ohlc *candle, size_t i, size_t n, size_t contraction);

inline signal conf_h(ohlc *candle, size_t i, size_t n, size_t contraction);